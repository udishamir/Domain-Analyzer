Domain Analyzer tries to detect malicious domain by the following methods:

1)it extract the ASN from the supplied domain name or ip address
2)it verify the domain is not part of the black ASN list
3)it verify if the domain is registered in alexa
4)it verify the landing page is not default installation/or what eve it configured.

Building
--------
gcc  -o urlanalyzer urlanalyzer-pcre.c asn.c getaddrinfo.c -lcurl -lGeoIP -lpcre

works on OSX, Linux.

Windows
-------
coming soon.......
